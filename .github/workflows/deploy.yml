name: CI/CD Pipeline Homelab

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy App using Docker Compose
        run: |
          docker compose up -d --build